// Tipo para servicios adicionales (limpieza, transporte, etc.)
export interface Service {
  id: string;
  name: string;
  description: string;
  icon: string;         // Ruta del icono (SVG o PNG)
}
